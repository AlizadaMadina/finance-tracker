# finance-tracker requirements
# Core dependencies only — stdlib is used where possible
flask
gunicorn
# CSV parsing, data analysis
pandas>=2.0.0

# Visualization (optional - for future chart extensions)
matplotlib>=3.7.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
